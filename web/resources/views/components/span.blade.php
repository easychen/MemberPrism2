<span class="relative inline-block px-3 py-1 {{$color}} font-semibold leading-tight {{$class}}">
    <span aria-hidden
        class="absolute inset-0 opacity-50 {{$bg}} rounded"></span>
    <span class="relative">{{ $slot }} </span>
</span>
